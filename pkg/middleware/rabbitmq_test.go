package middleware

import (
	"testing"
)

// This test file ensures backward compatibility is maintained
// All tests have been migrated to pkg/middleware/mq/rabbitmq_test.go

func TestForwardCompatibility(t *testing.T) {
	// This is just a placeholder test to ensure the package can be built
	// All real testing happens in the mq package
}
